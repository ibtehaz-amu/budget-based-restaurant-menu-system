# ? XAMPP MYSQL SETTINGS
spring.datasource.url=jdbc:mysql://localhost:3306/restaurantmenu?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=

# ? HIBERNATE SETTINGS
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ? SECURITY ENABLE KARO (JWT ke liye zaroori)
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Server Port
server.port=8081

# JWT Settings
jwt.secret=restaurantBudgetMenuSecretKeyForJWTTokenGeneration2024With32Chars
jwt.expiration=86400000

# ? DEBUG LOGGING
logging.level.com.restaurant.backend=DEBUG